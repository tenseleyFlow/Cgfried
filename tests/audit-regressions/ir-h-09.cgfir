// XFAIL(audit): IR-H-09 oversized initializer length wraps before validation
// The textual initializer is intentionally tiny. Its declared byte size makes
// `1 + size * 2` wrap, so the current parser attempts an enormous allocation
// instead of rejecting the length mismatch normally.
global @g size 9223372036854775808 align 8 external init x
