// Check 1: the defining block must dominate the use.
// ERROR_EXPECTED: ir verify [1]
func void @f(i32 %c) {
entry():
    condbr %c, a(), b()
a():
    %x = iadd i32 1, 2
    br join()
b():
    br join()
join():
    %y = iadd i32 %x, 1
    ret
}
