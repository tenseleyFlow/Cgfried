// FLAGS: -O1
// IR_CHECK-NOT: alloca
// IR_CHECK-NOT: load
// IR_CHECK-NOT: store
func ptr @f(ptr %x) {
entry():
    %p = alloca 8, align 8
    store ptr %x, %p, align 8
    %v = load ptr, %p, align 8
    ret ptr %v
}
