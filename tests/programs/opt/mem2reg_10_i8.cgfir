// FLAGS: -O1
// IR_CHECK-NOT: alloca
// IR_CHECK-NOT: load
// IR_CHECK-NOT: store
func i8 @f(i8 %x) {
entry():
    %p = alloca 1, align 1
    store i8 %x, %p, align 1
    %v = load i8, %p, align 1
    ret i8 %v
}
