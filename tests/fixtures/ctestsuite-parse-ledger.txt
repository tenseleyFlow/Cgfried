# c-testsuite parse differential: files that do NOT yet agree with gcc.
# Format: <file><TAB><ledger-id><TAB><diagnostic fingerprint><TAB><reason>
# XPASS is a failure: if a file starts agreeing, delete its line.
#
# XD-S12-SIZEOF — RETIRED in Sprint 15: sizeof in a constant expression
#   folds now (Sprint 14 gave it layout, Sprint 15 the evaluator), so the
#   file that needed it agrees with gcc and its entry is gone.
#
# TI-M-03: the fingerprint is matched literally against Cgfried's diagnostic.
# A filename that still disagrees for a different reason is new debt, not a
# continuation of the listed deferral.
#
00216.c	XD-S10-GNUEXT	GNU no-named-member extension is not supported	GNU empty records remain an explicit v0.1.0 refusal after range designators were implemented
