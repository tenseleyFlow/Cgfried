# The highest sprint number that is CLOSED.
#
# scripts/check_deferrals.sh rejects any user-visible message in src/ that
# defers work to a sprint at or below this number. A deferral naming a closed
# sprint is worse than an open one: it reads as tracked when nothing tracks
# it, and nobody reading the roadmap will ever find it.
#
# The roadmap itself (.docs/sprints/) is not in the repository, so this file
# is the in-tree source of truth. Raise it when a sprint closes -- and expect
# to fix or reword some deferrals in the same commit, which is the point.
#
# One bare integer, comments and blank lines ignored.
51
