# Sprint 58 activates controlled O2 stage1 timing on the Linux fleet.
name=stage1-self-time
state=trial
where=fleet:kasumi,hasu
when=nightly
threshold=>30%-median-wall-or-user+sys
rationale=Self-compilation catches whole-compiler throughput regressions on controlled hosts
quiet_days=14
false_trip_limit=2
false_trip_window_days=30
owner_sprint=Sprint 54
