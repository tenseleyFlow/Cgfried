# Sprint 57 made the pinned hybrid full-build lane green.  Keep its expensive
# timing isolated from the already-blocking Sprint 52 compile baseline while
# it earns the standard quiet streak on the two Linux fleet hosts.
name=musl-full-build
state=trial
where=fleet:kasumi,hasu
when=nightly
threshold=>30%-median-wall-or-user+sys-and->20%-max-rss
rationale=The pinned hybrid musl build is a fleet-only macro benchmark
quiet_days=14
false_trip_limit=2
false_trip_window_days=30
owner_sprint=Sprint 54
