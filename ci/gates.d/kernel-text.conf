# New Sprint 54 padding/layout backstop; collect a quiet streak first.
name=kernel-text
state=trial
where=shared-ci,arm64-ci
when=every-pr
threshold=>5%-per-kernel
rationale=Text bytes catch padding and layout bloat hidden by instruction counts
quiet_days=14
false_trip_limit=2
false_trip_window_days=30
owner_sprint=Sprint 54
