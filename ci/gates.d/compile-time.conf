# Sprint 52's established fleet-only compiler-throughput gate.
name=compile-time
state=blocking
where=fleet-only; shared-ci=never
when=nightly-and-pre-release
threshold=>30%-median-wall-or-user+sys
rationale=Wall time is scheduler-sensitive and needs the fleet's controlled hosts
owner_sprint=Sprint 54
