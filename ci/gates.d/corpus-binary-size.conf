# New Sprint 54 deterministic gate; collect a quiet streak before promotion.
name=corpus-binary-size
state=trial
where=shared-ci,arm64-ci
when=every-pr
threshold=>15%-stripped-per-program
rationale=Stripped bytes expose deterministic generated-program bloat
quiet_days=14
false_trip_limit=2
false_trip_window_days=30
owner_sprint=Sprint 54
