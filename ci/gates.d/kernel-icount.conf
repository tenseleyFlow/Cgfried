# Sprint 53 established this deterministic gate before trial-mode existed.
name=kernel-icount
state=blocking
where=shared-ci,arm64-ci
when=every-pr
threshold=>max(2%,2-instructions)-per-kernel
rationale=Symbol-bounded instruction count is deterministic across runners
owner_sprint=Sprint 54
