# Diagnostic counters are visible but never gate implementation choices.
name=internal-stats
state=report-only
where=everywhere
when=every-pr
threshold=report-only
rationale=Arena peaks and interner hits diagnose regressions without ossifying internals
owner_sprint=Sprint 54
