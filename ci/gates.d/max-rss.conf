# Sprint 52's stable memory ceiling remains blocking everywhere.
name=max-rss
state=blocking
where=shared-ci,arm64-ci,fleet
when=every-pr
threshold=>20%-per-compile-lane
rationale=Peak RSS is stable enough to compare on shared runners
owner_sprint=Sprint 54
