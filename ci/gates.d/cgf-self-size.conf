# Compiler-binary growth is independent from generated-program growth.
name=cgf-self-size
state=trial
where=shared-ci,arm64-ci
when=every-pr
threshold=>15%-stripped
rationale=The stripped compiler binary makes implementation bloat visible
quiet_days=14
false_trip_limit=2
false_trip_window_days=30
owner_sprint=Sprint 54
