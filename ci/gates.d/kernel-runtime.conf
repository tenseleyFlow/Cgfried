# Sprint 54 fleet truth lane. Promotion requires runtime_gate.sh --promote.
name=kernel-runtime
state=trial
where=fleet
when=nightly
threshold=>10% and >4*max(MAD_base,MAD_new)
rationale=Fleet-only runtime truth lane with a robust noise guard
quiet_days=14
false_trip_limit=2
false_trip_window_days=30
owner_sprint=Sprint 54
